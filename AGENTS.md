#Agent 开发规范

## 1. 项目概述

本项目是基于React + TypeScript + Tailwind CSS开发的Java后端个人作品集网站，核心目标是简洁、专业地展示个人技术栈、实战项目成果和联系方式，适配PC/移动端，支持免费部署，代码可复用、易维护，贴合Java后端开发者的技术展示需求。

## 2. 开发规范（必严格遵循）

- 组件开发：统一使用函数式组件 + React Hooks（如useState、useEffect、useRef），拒绝类组件；组件拆分颗粒度合理，实现高复用性（如公共按钮、导航栏）。
- 样式规范：全部使用Tailwind CSS编写样式，不写自定义CSS（特殊情况除外）；严格遵循深色主题规范（背景#0a0a0a，文字#ffffff，强调色渐变）；样式命名清晰，贴合组件功能。
- 代码规范：        
  - TypeScript类型定义完整，避免any类型；
  - 代码缩进统一（2个空格），变量、函数、组件命名采用驼峰命名法；
  - 关键逻辑、组件功能添加注释（// 组件用途、// 核心逻辑说明），便于后续维护和展示；
  - 使用eslint + prettier规范代码格式，避免语法错误和冗余代码。
- 文件目录：严格遵循TECH_DESIGN中定义的项目目录结构，不随意新增、修改目录名称；静态资源分类存放，图片、图标统一放入assets目录。

## 3. 设计要求（细化执行标准）

- 深色主题：严格遵循背景色#0a0a0a，文字色#ffffff，强调色使用蓝紫渐变（from-blue-500 to-purple-600），按钮、链接、技能标签等元素使用强调色，hover时颜色加深。
- 动画效果：         
  - 滚动动画：页面内跳转使用平滑滚动，导航栏滚动时背景透明度从0变为0.8（深色半透明）；
  - 元素动画：首页元素（标题、简介、头像）依次入场（Framer Motion，duration: 0.8s），项目卡片hover时缩放1.03倍、添加阴影，技能列表hover时轻微上浮；
  - 动画原则：自然、流畅，不突兀，不影响页面加载速度和用户体验。
- 移动端适配：         
  - 导航栏：移动端转为汉堡菜单（点击展开/收起），避免导航项溢出；
  - 布局：项目卡片、技能列表在移动端改为单列布局，间距调整适配屏幕；
  - 交互：按钮、链接尺寸放大，便于移动端点击，避免误触。

## 4. 核心开发任务分工（多Agent协作）

### Agent1：项目初始化Agent

- 职责：初始化Vite + React + TypeScript项目，安装所有依赖（Tailwind CSS、React Router、Framer Motion等），配置tsconfig.json、tailwind.config.js、vite.config.js，搭建基础目录结构。
- 输出：可正常运行的项目基础骨架，所有依赖安装完成，配置文件调试无误。

### Agent2：组件开发Agent

- 职责：按照TECH_DESIGN中的组件拆分，开发所有组件（公共组件、Hero、About、Projects、Contact、Footer），实现组件UI和基础交互；引入静态数据，完成数据渲染。
- 输出：所有组件代码（带完整注释），组件可正常渲染，数据展示无误；组件样式符合深色主题规范。

### Agent3：动画与交互Agent

- 职责：使用Framer Motion实现所有动画效果（入场、hover、滚动动画）；实现导航栏平滑跳转、图片懒加载；调试动画流畅度，确保不卡顿。
- 输出：动画效果全部实现，交互逻辑正常；图片懒加载功能生效，滚动动画流畅。

### Agent4：调试与优化Agent

- 职责：调试组件兼容性（不同浏览器、不同设备）；修复代码bug（路由跳转、数据渲染、动画异常等）；优化页面性能（图片压缩、代码精简）；检查所有链接是否可正常点击。
- 输出：bug修复清单，优化后的代码，页面性能达标，兼容性无问题。

### Agent5：部署与文档Agent

- 职责：编写项目打包、部署教程（Gitee Pages/GitHub Pages + 服务器部署）；整理项目说明文档（README.md），包含项目介绍、技术栈、部署步骤、代码结构；打包项目，生成dist目录。
- 输出：部署教程、README.md文档、dist打包目录，确保部署流程可直接操作。

## 5. 开发执行流程（按顺序推进）

1. Agent1：初始化项目骨架 → 完成依赖安装和配置；
2. Agent2：开发所有组件 → 引入静态数据，完成UI渲染；
3. Agent3：添加动画和交互 → 调试动画效果，实现图片懒加载和平滑跳转；
4. Agent4：调试优化 → 修复bug，优化性能，检查兼容性和链接有效性；
5. Agent5：打包部署 → 编写部署教程和README，生成dist目录；
6. 最终交付：可正常运行、可部署的完整项目源码，以及所有相关文档。

## 6. 注意事项（重点强调）

- 设计层面：保持简洁，拒绝过度设计，突出Java后端技术展示的核心需求，避免冗余元素。
- 性能层面：图片必须使用懒加载，代码精简，避免冗余，确保页面加载速度快。
- 功能层面：所有链接必须可点击，跳转正常；移动端适配必须达标，无内容溢出、按钮不可点击等问题。
- 代码层面：严格遵循TypeScript规范，组件可复用，注释完整，便于后续修改和个人展示（面试官可清晰查看代码逻辑）。
- 部署层面：确保部署流程简单易操作，支持免费部署方案，方便个人快速上线。

## 7. 预留扩展指令（便于后期迭代）

- 后期可新增功能：技术博客模块、个人简历PDF导出功能、访客统计功能、留言功能。
- 扩展规范：新增功能时，需遵循现有目录结构和代码规范，组件拆分合理，不破坏原有功能和样式。