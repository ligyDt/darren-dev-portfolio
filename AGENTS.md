# Agent 开发规范

## 1. 项目概述

本项目是 Darren 的 Java 后端个人作品集网站。当前实现已经收敛为纯静态页面，只使用 HTML、CSS、少量原生 JavaScript 和本地静态资源。

目标是让页面可以直接双击 `index.html` 离线打开。

## 2. 当前技术边界

- 页面结构：HTML
- 样式：CSS
- 交互：原生 JavaScript
- 图标：内联 SVG
- 图片资源：本地文件
- 页面入口：`index.html`
- 静态资源目录：`assets/images/`

## 3. 文件维护规范

- 页面内容、样式、交互统一维护在 `index.html`。
- 图片和二维码统一放在 `assets/images/`。
- 文档变更需要同步 `README.md`、`PRD.md`、`TECH_DESIGN.md`、`AGENTS.md` 中对应事实。

## 4. 代码规范

- CSS 使用清晰的 class 命名和 `:root` 变量，常用值集中维护。
- JavaScript 只处理必要交互：页面滚动、移动端菜单、按钮打开外部入口。
- 图标优先使用内联 SVG。
- 外部链接统一使用 `button data-open="..."`。
- 页面内跳转统一使用 `button data-scroll="sectionId"`。
- 内容文案保持真实、平实，不写夸张指标和无法面试解释的描述。

## 5. 设计规范

- 整体风格：深色、简洁、工程展示感。
- 背景：深色网格背景。
- 强调色：绿色 `#00e5a0`。
- 移动端：导航可展开收起，项目卡片单列展示，文本不溢出。

## 6. 项目展示规范

- 只展示已实现、已确认、可说明清楚的内容。
- 私有项目不展示源码入口。
- 线上未开源项目只展示访问入口和确认状态，不扩展描述无法确认的功能。
- 不展示未实现、未验证、规划中、待完善、已知 Bug、风险或建议类内容。

## 7. 验证清单

- 页面能离线打开。
- 导航滚动正常。
- 项目按钮、邮箱、GitHub 入口正常。
- 微信二维码能显示。
- PC 和移动端布局没有明显溢出。

## 8. 后续扩展

如需新增内容，优先在现有 section 内补充，并同步更新相关文档。
